<svg width: '300px' height: '200px'>
        <function (answers) {
        if(answers.shape === 'Circle') {
            return circle;
        } else if (answers.shape === 'Square') {
            return rect;
        } else {
            return polygon;
        }
    } width: '300px' height: '200px' fill: 'blue' />
            <text x="50%" y="50%" fill="red" text-anchor="middle">xxx</text>
    </svg>     
    